# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# typescript
*.tsbuildinfo

# production
/build

# misc
.DS_Store

# env files — ignore every .env variant (incl. a bare .env) but keep the example
.env
.env.*
!.env.example

# vercel
.vercel

# service-account sleutels (GA4 MCP) — nooit committen
*service-account*.json
*-credentials.json
gcp-key*.json
